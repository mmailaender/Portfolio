const config = {
  themes: {
    productTheme: {
      fontFamily: {
        body: {
          family: 'Poppins, sans-serif',
        },
      },
    },
  },
  themeFragments: {
    toolTip: {
      color: {
        foregroundNeutral: { hex: '#ffffff', hexDark: '#000000' },
      },
    },
  },
};

module.exports = config;
